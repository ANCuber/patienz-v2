# PaTiENZ 升級方案（基於真實回饋 + 程式碼現況 + 權威資料驗證）

> 本文件不是把回饋表與會議記錄照抄成 to-do list，而是先**區分「已被解決的舊問題」與「仍在惡化的真實問題」**，再用程式碼實證找出根因，最後提出**經外部權威資料驗證、技術上完全可行**的方案。
>
> 證據來源：38 筆表單回饋（2025/01–2026/05）、`patienz-v2/` 實際程式碼、Google Gemini 官方能力、醫學影像 AI 文獻。

---

## 0. 一個關鍵前提：回饋橫跨 17 個月，必須分層看

回饋從 2025/1 到 2026/5。使用者已提醒「有些較舊的問題可能已被解決」。對照程式碼現況，**會議記錄與舊回饋裡有大量項目其實已經做完了**，若照單全收會浪費資源：

| 會議/舊回饋要求 | 程式碼現況 | 結論 |
|---|---|---|
| 「要選擇自己練習的身分」 | `config.py:114–141` 已有 `學員身份`（Pre-Clerk→Attending），並驅動 ACGME 期望等級 | **已完成** |
| 「先看到 vital signs」 | `problem_setter` schema 已含 `VitalSigns` 區塊 | **已完成（需確認 UI 呈現順序）** |
| 「初步鑑別診斷區不要放處置」 | `pre_ddx.py` 僅收 DDx+理由+可能性；處置在 `diagnosis.py` | **已分離** |
| 「分開生化檢查區/影像檢查區」 | `examination_choice.json` 已分 實驗室/影像/特殊…類別 | **已分類（問題在操作，不在分區）** |
| 「OSCE / ACGME」 | `grader_v2`（OSCE）+ `acgme_grader` 兩套**都已實作並都會跑** | **已完成，但缺『分流選擇』** |
| 「換模型加速」 | 已是 `gemini-2.5-flash` + `flash-lite`（比計畫書寫的 1.5 Flash 新兩代） | **已換，但慢的根因不是模型本身（見下）** |

> **真正仍存在、且在 2026 回饋中變嚴重的問題只有幾類**：①速度（壓倒性第一）②檢查項目覆蓋不足會卡死案例 ③檢查操作仍要一項一項點 ④評分語意/可信度 ⑤病人擬真與小兒一致性 ⑥影像仍是純文字。下面逐一給根因與方案。

---

## 1. 速度（最高優先，2026 回饋的壓倒性主訴）

### 真實證據
- 「問一個問題要等 2、3 分鐘才會回應」「速度太慢大部分時間在等待」「爬蟲時間長」「回覆很慢，**也沒有回答完**」（多筆 2026/5 PGY/Intern）。
- 滿意度統計列（第 41–42 列）「檢查區」分數 3.25，是所有面向最低，且明確綁定等待時間。

### 根因（程式碼實證，會議記錄誤判為「換模型」）
速度問題**不是模型太大**，而是三個架構性錯誤疊加：

1. **執行期即時 Selenium 爬 UpToDate → 轉 PDF → 上傳 Gemini Files**（`tools.py:157–213`，`patient.py:59–60`）。
   - 這就是「爬蟲時間長」與「第一次對話很久」的根因，也是「**公用網路無法使用此系統**」的元兇（公用網路擋 headless Chrome / 外連）。每個案例開一次無頭瀏覽器抓網頁，極慢且極脆弱。

2. **Gemini 2.5 Flash 預設開啟 thinking，而現用的 `google-generativeai` v0.8.6 舊 SDK 沒有 `thinking_config` 參數可關閉**。
   - 後果有二：(a) 每個病人回合都付 thinking token 的延遲；(b) 程式被迫把 `max_output_tokens` 開到 12k–16k 來避免 thinking token 把 JSON 擠爆（grader 系列）。這正好解釋「**回覆很慢，也沒有回答完**」＝在 MAX_TOKENS 被截斷。

3. **檢查與評分都是「逐項目、序列、阻塞」呼叫**：
   - 檢查每個 subcategory 一次 `value_examiner`/`text_examiner`（`examination.py:344–415`），使用者點一類等一次 → 「必須一個一個項目分開進行（＝等的時間更久）」。
   - 評分管線 4 次序列阻塞呼叫：`mark_scheme_setter → grader_v2 → acgme_grader → advisor`（`grade.py:494–608`），每次 12–16k token budget。

### 方案（皆經官方能力驗證，依投報率排序）

**1-A. 遷移到新版 `google-genai` SDK，對病人/檢查回合設 `thinking_budget=0`**（最高投報率，1–2 天）
- 官方：Gemini 2.5 Flash 為 hybrid thinking，thinking budget 可設 0–24576，**可關閉**。關閉後病人對話延遲大幅下降，且不再需要 16k token budget，杜絕截斷。
- 策略：病人對話、examiner 數值/影像 → `thinking_budget=0`；只有 `grader_v2`/`acgme_grader` 保留小額 thinking（例如 512–2048）以維持推理品質。

**1-B. 廢除執行期 Selenium 爬蟲，改用離線 RAG**（解決爬蟲延遲＋公用網路失敗，3–5 天）
- `knoledge_base/` 已有 Harrison's、CMDT、Talley、Oxford 等 PDF。**離線預先切塊建向量庫**（這正是計畫書的 SOP 向量庫，但要先做「移除 runtime 爬蟲」這一步）。
- 病人/advisor 改成從本地向量庫檢索疾病臨床特徵，**完全不再外連 UpToDate**。同時消除「公用網路無法使用」。

**1-C. 用 Gemini Context Caching 固定 case JSON＋system instruction**（降延遲＋省 90% 重複 token）
- 官方：context caching 對「大量重複內容」可降成本/延遲達 ~90%。整個 session 的 case JSON、評分 schema、病人行為規則都是每回合重送的固定大區塊 → 放進 explicit cache，每回合只送新訊息。

**1-D. 平行化 + 預載（會議「async/preloading」的正確做法）**
- **檢查**：把購物車內多個 subcategory 用 `asyncio`/thread pool **並發**呼叫，而非序列。
- **評分**：`grader_v2` 與 `acgme_grader` 互相獨立（都只依賴 student_data + 各自 schema）→ **並發跑**，評分時間近乎砍半。
- **真正的預載**：`mark_scheme_setter` **只依賴 case JSON**，案例一生成時 case JSON 就已知 → 在使用者還在問診時，**背景先把 mark scheme 產好**，等到評分頁時這段延遲已完全隱藏。這比會議講的「async」更徹底。

> ⚠️ 對會議「限時間/tokens 換模型 壓縮 prompt」的修正：換更小模型（flash-lite）只能小幅改善，且會犧牲病人擬真與評分品質；**真正的數量級加速來自 1-A（關 thinking）+ 1-B（去爬蟲）+ 1-D（平行/預載）**。壓縮 prompt 可做（1-C 已順帶處理），但不是主因。

---

## 2. 檢查項目覆蓋不足——會直接「卡死」案例（高優先）

### 真實證據
- 「病人 4 答案是 RA，結果**檢查不能抽 RF**，無法用 ACR/EULAR Criteria 診斷」
- 「抽血不能驗 Creatinine」「GI bleeding 無法驗 stool」「沒有腎功能、CRP、胃鏡、手部 X 光、KUB」「關節痛應有四肢 X-ray，目前只有胸腹部」

### 根因
`examination.csv` 是**固定目錄**（`examination.py:192–227` 以 `l:r` 列範圍對應）。固定目錄永遠追不上 `problem_setter` 動態生成的疾病所需的 workup → 學生想做的關鍵檢查不存在 → **案例邏輯斷裂、無法收斂到正確診斷**。這是比 UI 更嚴重的「正確性」問題。

### 方案
**2-A. 案例生成時保證 workup 完整性**（根本解）
- 讓 `problem_setter` 在 hidden data 裡同時輸出「達成正確診斷/鑑別所需的關鍵檢查清單（required workup）」，生成後**自動把這些項目注入該案例可選的檢查目錄**。RA 案例必有 RF/anti-CCP，GI bleeding 必有 stool occult blood。

**2-B. 「其他檢查（自由輸入）」逃生口**
- 檢查區加一個自由輸入欄，交給 `text_examiner` 依 case JSON 生成合理結果。即使目錄沒列，學生臨床上想做的檢查也能做，徹底消除「想做的檢查沒有」。

**2-C. 系統性擴充固定目錄**（腎功能、CRP、ESR、自體抗體組、糞便、內視鏡、四肢/KUB X 光等常用項目），作為 2-A/2-B 的基礎。

---

## 3. 檢查操作仍要逐項點、一次只能一區（高優先，便宜）

### 真實證據
- 「要一個一個點/可以設定組套」「CBC/DC 不應該一個一個點」「一次只能安排一個區域，希望一次安排所有檢查」「一開始不知道是一個大項目送出一次」。

### 根因
雖然 `const.default_all` 對部分 subcategory 預設全選，但**沒有類別層級的「全選」**，且每個 subcategory 仍各觸發一次 LLM 呼叫（與第 1 點的序列延遲互相加乘）。

### 方案
- **3-A. 組套（panel）**：把臨床常見組合做成一鍵套餐（如「常規入院抽血」＝CBC/DC+電解質+腎肝功能+CRP）。對應會議「點一個類別就要全選」。
- **3-B. 跨類別購物車一次送出**：購物車已存在（`examination.py:257–274`），改為「全部選完一次送出」，搭配 1-D 並發後端，把多次等待併成一次。
- **3-C. UI 明確說明**選取→送出的流程（多筆回饋誤解操作方式）。

---

## 4. 評分的語意可信度與透明度（高優先）

### 真實證據
- 「打到 **DMARD 和 NSAID**，和答案只差一個 s，就沒分」「**x 光攝影 和 x ray** 也沒分」
- 「**有做檢查但評分說我沒告知病人故沒計分**」「抽血按過的資訊不列入評分，要自己再講一遍」
- 「處置完成一項/二項/三項分開評分（重複計分）」「獲得 12/14 但整體只有 67 分？」「評分標準敘述不清楚」「很難及格，有簡易模式嗎」
- 一致需求：「希望有**詳解**／正確主診斷、鑑別、建議檢查」。

### 根因
`grader_v2` 是 LLM 評分（非字串比對），但 prompt 仍偏字面、且**未把『在檢查區下過的醫令』當成等同於『已執行該檢查』的證據**——`collect_student_data()` 雖有彙整 `examination_history`，但 grader 把「沒對病人口述」視為未做。

### 方案
**4-A. 行為＝證據**：明確指示 grader：**在檢查/理學區下了醫令本身即構成『已執行』的得分證據**，不需再口頭複述。直接修正「有做卻不給分」這個最傷信任的問題。

**4-B. 概念對應（mapping，呼應會議「OSCE_grader 用 mapping」）**：評分前把學生的診斷/處置/檢查自由文字先**標準化映射到概念**（計畫書已規劃 ICD-10／SNOMED CT）。DMARD≡DMARDs、x-ray≡X 光攝影、肺栓塞≡PE 在概念層就等價，根除「差一個 s 不給分」。

**4-C. 去重**：`mark_scheme_setter` prompt 加約束，禁止對同一處置拆成「完成一項/二項/三項」重疊計分。

**4-D. 透明詳解頁**：評分後直接呈現**標準答案**（正確主診斷、鑑別、建議 workup、建議處置）＋逐項「你做了什麼 vs 標準」對照。這同時滿足大量「希望有詳解」與計畫書的「事後逐步引導」。

**4-E. 分數換算說明＋難度模式**：解釋「12/14→67 分」的權重換算；提供「簡易/標準」評分嚴格度切換（回應「很難及格」）。

---

## 5. 病人擬真與一致性（中高優先）

### 真實證據
- 「症狀太典型，問到就馬上說有」「太容易把 LQQOPERA 全講出來」←→ 也有「患者講太少、問一個答一個」（需個性參數化）
- 「病人前後矛盾」「**持續顯示『不舒服無法回答』，無法問診**」（死結 bug）「回答鬼打牆」「出現簡體字」
- 「**兒科病患標註 53 歲、生日 2013、回答 11 歲、無家屬陪伴不合理**」（小兒一致性嚴重 bug）

### 根因
- 「不舒服無法回答」死結＝`diagnosis.py:30–70` 對空/被擋回應的 fallback 不斷觸發（與第 1 點 thinking 截斷、safety block 相關），且 fallback 文案把責任推給使用者又無法復原。
- 小兒：`_validate_birthday` 只校驗生日，未校驗「年齡↔生日↔敘事↔是否需家屬代答」的整體一致性。

### 方案
**5-A. 修死結**：1-A 關 thinking 後截斷消失；再放寬 safety（病人 agent 已有 `BLOCK_ONLY_HIGH`），並把 fallback 改為「自動重試＋不指責使用者」的復原邏輯。

**5-B. 擬真參數化**：把「健談度 / 情緒（焦慮、不耐）/ 是否主動吐露」做成 case 參數（會議與回饋都要：「設定個性機車一點的病人」）。預設**降低主動揭露**（只答被問的、不自動跑完 LQQOPERA），對應「典型到不真實」。

**5-C. 一致性**：用 1-C context caching 把 case JSON 釘在每回合上下文，並強規則「**絕不與已確立事實矛盾**」；簡體字以 prompt 強制繁中＋輸出後簡轉繁過濾。

**5-D. 小兒模式**：年齡可下修到 0 歲（已被多次要求）；生成後做**整體一致性硬檢查**（年齡↔生日↔身高體重↔敘事），未成年自動加入「家屬代答」角色設定。

---

## 6. 影像檢查——對會議「生圖 agent」的重要修正（中優先）

### 真實證據
- 「心電圖、X 光可以放實際圖片練習判讀，新增 MRI/MRA」「心電圖能有真的圖片更好」「stable diffusion 結合醫學？」目前影像是純文字（`examination.py:28` `TEXT_TYPE_EXAMS`）。

### 權威驗證（為何不能用生成模型做診斷影像）
- 放射科醫師辨識**合成胸部 X 光**的正確率僅 **62–78%**，且與年資無關（AI-generated 影像足以騙過專家）。
- 生成式報告/影像存在**幻覺出病灶（hallucinated pathology）**的已知風險；醫療領域錯誤資訊風險極高。

> 結論：**用 Nano Banana / Stable Diffusion 生成 ECG/CXR/CT 來訓練判讀，等於教學生讀「捏造的病灶」，方向錯誤。** 學生會把不存在的所見當成真。

### 方案（驗證為正確的做法：檢索真實影像，而非生成）
**6-A. 真實影像庫 + 檢索**：建立「(疾病/所見) → 真實去識別化影像」對應庫，由 examiner 已決定的 normal/abnormal 與目標 DDx 來檢索：
- ECG：**PTB-XL**（PhysioNet，開放）
- CXR：**NIH ChestX-ray14**、**Open-i / Indiana University CXR**（開放）；**MIMIC-CXR / CheXpert**（需 credentialing）
- 顯示真實影像＋既有文字報告，直接滿足「想練判讀真圖」。

**6-B. 生成式僅限非診斷性視覺**：Nano Banana（`gemini-2.5-flash-image`）等可用於皮疹外觀、衛教示意等**非安全關鍵**素材，且須標註 SynthID／「示意非真實」。診斷關鍵影像一律走 6-A。

---

## 7. 流程彈性——檢查與問診應可交錯（中優先）

### 真實證據
- 「不要要求一定問診完才能做檢查，臨床上先做檢查可幫助鑑別」「希望做完檢查後可以補問診」「可以檢查完後繼續問診」。
- 也有人要更模組化：「闖關型式模組區塊化考驗 thinking process」「先有 tentative diagnosis 再做 screening」。

### 根因
`tools.py:22–27` 單向前進 + `pre_ddx` 鎖定，把「問診→理學→初步鑑別→檢查→診斷」變成硬牆。

### 方案
- **7-A. 自由切換**：在「問診／理學／檢查」三區之間允許自由往返（這三者本就應交錯），`pre_ddx` 改為**可回頭修改的檢查點**而非鎖死的牆。
- **7-B.（選配）闖關模式**：提供「臆斷→screening→再鑑別→確診」的分階段模式作為另一種練習型態，回應「不只是一次性標準答案」的進階需求。

---

## 8. UI 快速勝利（低成本、高滿意度）

全部來自多筆重複回饋，工時小：
- **Enter / Ctrl+Enter 直接送出**（出現最多次）；送出後**自動清空輸入框**。
- 對談紀錄**可上滑**；**計時器**（`tools.py:71–90` 已有 fragment timer，補齊呈現）。
- **切換視窗導致串流中斷**：避免單一阻塞串流相依，改為可恢復狀態（與 1-A 一併處理）。
- 部署於正式 HTTPS host，解決「公用網路無法使用」。

---

## 9. 評分一致性 + 分流（會議「scheme consistent / 從一開始分流」的正確實作）

- **9-A. Grading scheme 重用庫**：`mark_scheme_setter` 目前每案重生 → 以 (疾病, 學員身份, 教學重點) 為 key 建快取庫，相似則重用、否則新生。**同時提升一致性（會議「scheme consistent」）與速度**，並與 1-D 預載合併。
- **9-B. OSCE / ACGME 開頭分流**：目前兩套**都跑**＝雙倍評分延遲。讓使用者在 `config` 選擇要哪一套（或兩套），預設單套。直接砍掉多數人一半的評分等待，落實會議「從一開始就分流、選定 OSCE 或 ACGME」。

---

## 10. 優先順序與路線圖

| 階段 | 項目 | 影響 | 估時 |
|---|---|---|---|
| **P0 速度急救** | 1-A 關 thinking + 遷移 SDK｜1-B 去 Selenium 改離線 RAG｜1-D 評分並發+mark scheme 預載｜9-B OSCE/ACGME 分流 | 解決壓倒性第一主訴；同時修「回答不完整」「公用網路」 | 1–1.5 週 |
| **P0 正確性** | 2-A/2-B 檢查覆蓋+自由輸入｜4-A 行為即證據｜5-A 修「不舒服」死結｜5-D 小兒一致性 | 修「卡死案例」「有做不給分」「兒科 bug」等破壞性問題 | 1 週 |
| **P1 體驗** | 3-A/3-B 組套+一次送出｜8 UI 快速勝利｜4-D 詳解頁 | 大幅拉高 SUS 與滿意度 | 1 週 |
| **P1 品質** | 4-B mapping 評分｜4-C 去重｜5-B/5-C 擬真+一致性｜1-C context caching｜9-A scheme 重用庫 | 評分公正性、病人真實感、再降延遲 | 1.5–2 週 |
| **P2 進階** | 6-A 真實影像庫｜6-B 生成限非診斷｜7-A/7-B 流程彈性 | 滿足影像判讀與臨床思路訓練 | 2–4 週 |

---

## 附錄：與會議記錄的對照與修正

| 會議主張 | 本方案的處置 |
|---|---|
| 換模型 / 限 token 加速 | **部分採納但非主因**；真正加速＝關 thinking + 去爬蟲 + 並發/預載（§1） |
| 生圖 agent 做影像 | **修正**：診斷影像用真實影像庫檢索（§6），生成僅限非診斷性，因合成影像會教出捏造病灶 |
| 點一類別就全選 | 採納並升級為「組套 + 跨類別一次送出」（§3） |
| 存 grading scheme 資料庫、相似就拿 | 採納，以 (疾病,身份,重點) 為 key（§9-A） |
| 從一開始分流 OSCE/ACGME | 採納，config 選擇（§9-B） |
| 選練習身分 / vital signs 先看 / 鑑別區不放處置 / 生化影像分區 | **多數已實作**（§0），僅需確認 UI 呈現 |
| async / preloading | 採納並指明「mark scheme 可在問診時背景預生成」的正確切入點（§1-D） |
